import assert from'node:assert/strict';import{circleCircle,circleRect,circleSegment,collides,phaseAt,finalSafe}from'./game-core.mjs';
const p={x:50,y:50,r:10};assert.equal(circleCircle(p,{x:69,y:50,r:10}),true);assert.equal(circleCircle(p,{x:71,y:50,r:10}),false);assert.equal(circleRect(p,{x:58,y:45,w:20,h:20}),true);assert.equal(circleRect(p,{x:61,y:45,w:20,h:20}),false);assert.equal(circleSegment(p,{x1:0,y1:50,x2:100,y2:50,width:4}),true);assert.equal(circleSegment(p,{x1:0,y1:70,x2:100,y2:70,width:4}),false);assert.equal(collides(p,[{kind:'circle',x:50,y:50,r:2,red:false}]),null);assert.ok(collides(p,[{kind:'circle',x:50,y:50,r:2,red:true}]));assert.equal(phaseAt(0),0);assert.equal(phaseAt(14.99),0);assert.equal(phaseAt(15),1);assert.equal(phaseAt(109.9),7);assert.equal(phaseAt(110),8);assert.equal(phaseAt(120),8);assert.equal(finalSafe(p,{x:50,y:50,r:40},true),false);assert.equal(finalSafe(p,{x:100,y:100,r:20},true),true);console.log('15 collision and timeline assertions passed');
